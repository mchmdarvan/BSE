<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="personal.mine.bse">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".modul.BukuNonkurikulumActivity"></activity>
        <activity android:name=".modul.PaketCActivity" />
        <activity android:name=".modul.PaketBActivity" />
        <activity android:name=".modul.PaketAActivity" />
        <activity android:name=".modul.SmkActivity" />
        <activity android:name=".modul.SmaActivity" />
        <activity android:name=".modul.SmpActivity" />
        <activity android:name=".modul.SdActivity" />
        <activity android:name=".modul.PaudActivity" />
        <activity android:name=".modul.BukuNonkurikulumActivity" />
        <activity android:name=".modul.BukuPopulerActivity" />
        <activity android:name=".modul.BukuBaruActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".onBoarding.IntroActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>